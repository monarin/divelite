1. Create py2 conda environment ana-env-py2.yml.
   - To peek the env: 
     source /reg/g/psdm/etc/psconda.sh
     conda info
     vi /reg/g/psdm/sw/conda/inst/miniconda2-prod-rhel7/.condarc (for channels)
     conda env export > environment.yml
     # Change name of and prefix in environment.yml
     conda env create -f environment.yml
     # Note ndarray has to come from psana-rhel7 (file channel)
2. scon conda-install 
    - conda_install.py writes SConScript
    - standardExternalPackage
3. Autogenerated boost with ddl

